# Runtime dependencies. Kept alongside pyproject.toml so the service can be
# started with a plain `pip install -r requirements.txt` on a clean machine.
fastapi>=0.115
uvicorn[standard]>=0.32
httpx>=0.27
pydantic>=2.9
pydantic-settings>=2.6
