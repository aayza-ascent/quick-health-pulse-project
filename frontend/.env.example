# Where the FastAPI service is reachable from the Next.js server.
#
# Deliberately not a NEXT_PUBLIC_* variable: all API calls are made from the
# server component, so the browser never learns this URL and cannot be pointed
# at Junction directly.
HEALTH_PULSE_API_URL=http://localhost:8000
